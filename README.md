# -_SnakeOracle_19c_- :.
# 🐍 Snake Oracle 19c:

<img width="1254" height="1254" alt="image" src="https://github.com/user-attachments/assets/5fcb4989-754d-426d-9fb1-a80bce5d5e60" />  

```
  
Juego clásico **Snake** desarrollado en **C# Windows Forms** utilizando **Visual Studio 2026**, **.NET 10** y **Oracle Database 19c**.

El proyecto permite jugar Snake, calcular la puntuación y almacenar las partidas en Oracle 19c.

---

# 📋 Tecnologías

* Visual Studio 2026
* C#
* .NET 10
* Windows Forms
* Oracle Database 19c
* Oracle.ManagedDataAccess
* SQL
* Arquitectura por capas sencilla

---

# 🎯 Funcionalidades

* Ingresar nombre del jugador.
* Iniciar partida.
* Movimiento mediante teclado.
* Comida aleatoria.
* Crecimiento de la serpiente.
* Sistema de puntuación.
* Detección de colisiones.
* Game Over.
* Reiniciar partida.
* Guardar partidas en Oracle.
* Consultar ranking.
* Mostrar Top 10 jugadores.

---

# 📁 Estructura del proyecto

```text
SnakeOracle19C
│
├── Data
│   ├── ConexionOracle.cs
│   └── PartidaDAO.cs
│
├── Game
│   ├── Direccion.cs
│   ├── Serpiente.cs
│   └── Comida.cs
│
├── Models
│   ├── Jugador.cs
│   └── Partida.cs
│
├── Forms
│   ├── Form1.cs
│   ├── Form1.Designer.cs
│   ├── FrmRanking.cs
│   └── FrmRanking.Designer.cs
│
├── Program.cs
├── SnakeOracle19C.csproj
└── README.md
```

---

# 🗄️ 1. Crear la base de datos Oracle 19c

Conectarse desde SQL Developer, SQL Plus o la herramienta que utilices.

Crear las tablas:

```sql
CREATE TABLE JUGADORES (
    ID_JUGADOR NUMBER GENERATED BY DEFAULT AS IDENTITY,
    NOMBRE VARCHAR2(100) NOT NULL,
    FECHA_REGISTRO DATE DEFAULT SYSDATE,

    CONSTRAINT PK_JUGADORES
        PRIMARY KEY (ID_JUGADOR)
);
```

Crear tabla de partidas:

```sql
CREATE TABLE PARTIDAS (
    ID_PARTIDA NUMBER GENERATED BY DEFAULT AS IDENTITY,
    ID_JUGADOR NUMBER NOT NULL,
    PUNTUACION NUMBER DEFAULT 0,
    LONGITUD_SERPIENTE NUMBER DEFAULT 1,
    RESULTADO VARCHAR2(20),
    FECHA_PARTIDA DATE DEFAULT SYSDATE,

    CONSTRAINT PK_PARTIDAS
        PRIMARY KEY (ID_PARTIDA),

    CONSTRAINT FK_PARTIDAS_JUGADORES
        FOREIGN KEY (ID_JUGADOR)
        REFERENCES JUGADORES(ID_JUGADOR)
);
```

---

# 🔎 2. Consultar las tablas

```sql
SELECT *
FROM JUGADORES;
```

```sql
SELECT *
FROM PARTIDAS;
```

---

# 🏆 3. Consulta del ranking

```sql
SELECT
    J.NOMBRE,
    P.PUNTUACION,
    P.LONGITUD_SERPIENTE,
    P.RESULTADO,
    P.FECHA_PARTIDA
FROM PARTIDAS P
INNER JOIN JUGADORES J
    ON J.ID_JUGADOR = P.ID_JUGADOR
ORDER BY P.PUNTUACION DESC;
```

---

# 📦 4. Crear proyecto

En Visual Studio 2026:

```text
Create a new project
```

Seleccionar:

```text
Windows Forms App
```

Lenguaje:

```text
C#
```

Framework:

```text
.NET 10
```

Nombre:

```text
SnakeOracle19C
```

---

# 📦 5. Instalar Oracle.ManagedDataAccess

Abrir:

```text
Tools
→ NuGet Package Manager
→ Package Manager Console
```

Ejecutar:

```powershell
Install-Package Oracle.ManagedDataAccess
```

También se puede instalar desde:

```text
Manage NuGet Packages
```

Buscar:

```text
Oracle.ManagedDataAccess
```

---

# ⚙️ 6. SnakeOracle19C.csproj

```xml
<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>WinExe</OutputType>
    <TargetFramework>net10.0-windows</TargetFramework>
    <UseWindowsForms>true</UseWindowsForms>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="Oracle.ManagedDataAccess" Version="23.9.1" />
  </ItemGroup>

</Project>
```

> Si NuGet muestra una versión estable más reciente, puedes utilizar esa versión.

---

# 🔌 7. Data/ConexionOracle.cs

```csharp
using Oracle.ManagedDataAccess.Client;

namespace SnakeOracle19C.Data
{
    public class ConexionOracle
    {
        private readonly string connectionString =
            "User Id=SYSTEM;" +
            "Password=TU_PASSWORD;" +
            "Data Source=localhost:1521/orcl;";

        public OracleConnection ObtenerConexion()
        {
            return new OracleConnection(connectionString);
        }
    }
}
```

Cambiar:

```text
TU_PASSWORD
```

por la contraseña de Oracle.

Por ejemplo:

```csharp
"User Id=SYSTEM;" +
"Password=oracle;" +
"Data Source=localhost:1521/orcl;";
```

Si utilizas otro usuario:

```csharp
"User Id=SNAKE;" +
"Password=12345;" +
"Data Source=localhost:1521/orcl;";
```

---

# 👤 8. Models/Jugador.cs

```csharp
namespace SnakeOracle19C.Models
{
    public class Jugador
    {
        public int IdJugador { get; set; }

        public string Nombre { get; set; } = string.Empty;

        public DateTime FechaRegistro { get; set; }
    }
}
```

---

# 🎮 9. Models/Partida.cs

```csharp
namespace SnakeOracle19C.Models
{
    public class Partida
    {
        public int IdPartida { get; set; }

        public int IdJugador { get; set; }

        public int Puntuacion { get; set; }

        public int LongitudSerpiente { get; set; }

        public string Resultado { get; set; } = string.Empty;

        public DateTime FechaPartida { get; set; }
    }
}
```

---

# 🧭 10. Game/Direccion.cs

```csharp
namespace SnakeOracle19C.Game
{
    public enum Direccion
    {
        Arriba,
        Abajo,
        Izquierda,
        Derecha
    }
}
```

---

# 🐍 11. Game/Serpiente.cs

```csharp
using System.Drawing;

namespace SnakeOracle19C.Game
{
    public class Serpiente
    {
        public List<Point> Cuerpo { get; private set; }

        public Direccion DireccionActual { get; set; }

        public Serpiente(int x, int y)
        {
            Cuerpo = new List<Point>
            {
                new Point(x, y),
                new Point(x - 1, y),
                new Point(x - 2, y)
            };

            DireccionActual = Direccion.Derecha;
        }

        public Point ObtenerCabeza()
        {
            return Cuerpo[0];
        }

        public void Mover(bool crecer)
        {
            Point cabeza = ObtenerCabeza();

            Point nuevaCabeza = cabeza;

            switch (DireccionActual)
            {
                case Direccion.Arriba:
                    nuevaCabeza = new Point(cabeza.X, cabeza.Y - 1);
                    break;

                case Direccion.Abajo:
                    nuevaCabeza = new Point(cabeza.X, cabeza.Y + 1);
                    break;

                case Direccion.Izquierda:
                    nuevaCabeza = new Point(cabeza.X - 1, cabeza.Y);
                    break;

                case Direccion.Derecha:
                    nuevaCabeza = new Point(cabeza.X + 1, cabeza.Y);
                    break;
            }

            Cuerpo.Insert(0, nuevaCabeza);

            if (!crecer)
            {
                Cuerpo.RemoveAt(Cuerpo.Count - 1);
            }
        }

        public bool ChocaConSuCuerpo()
        {
            Point cabeza = ObtenerCabeza();

            for (int i = 1; i < Cuerpo.Count; i++)
            {
                if (Cuerpo[i] == cabeza)
                {
                    return true;
                }
            }

            return false;
        }
    }
}
```

---

# 🍎 12. Game/Comida.cs

```csharp
using System.Drawing;

namespace SnakeOracle19C.Game
{
    public class Comida
    {
        private readonly Random random = new Random();

        public Point Posicion { get; private set; }

        public void Generar(int columnas, int filas, List<Point> cuerpo)
        {
            do
            {
                Posicion = new Point(
                    random.Next(0, columnas),
                    random.Next(0, filas)
                );

            } while (cuerpo.Contains(Posicion));
        }
    }
}
```

---

# 💾 13. Data/PartidaDAO.cs

```csharp
using Oracle.ManagedDataAccess.Client;
using SnakeOracle19C.Models;

namespace SnakeOracle19C.Data
{
    public class PartidaDAO
    {
        private readonly ConexionOracle conexion;

        public PartidaDAO()
        {
            conexion = new ConexionOracle();
        }

        public int ObtenerOCrearJugador(string nombre)
        {
            using OracleConnection connection = conexion.ObtenerConexion();

            connection.Open();

            string sqlBuscar = @"
                SELECT ID_JUGADOR
                FROM JUGADORES
                WHERE UPPER(NOMBRE) = UPPER(:NOMBRE)
                FETCH FIRST 1 ROW ONLY";

            using OracleCommand commandBuscar =
                new OracleCommand(sqlBuscar, connection);

            commandBuscar.Parameters.Add(
                ":NOMBRE",
                OracleDbType.Varchar2
            ).Value = nombre;

            object? resultado = commandBuscar.ExecuteScalar();

            if (resultado != null && resultado != DBNull.Value)
            {
                return Convert.ToInt32(resultado);
            }

            string sqlInsertar = @"
                INSERT INTO JUGADORES
                (
                    NOMBRE,
                    FECHA_REGISTRO
                )
                VALUES
                (
                    :NOMBRE,
                    SYSDATE
                )
                RETURNING ID_JUGADOR INTO :ID";

            using OracleCommand commandInsertar =
                new OracleCommand(sqlInsertar, connection);

            commandInsertar.Parameters.Add(
                ":NOMBRE",
                OracleDbType.Varchar2
            ).Value = nombre;

            OracleParameter parametroId =
                new OracleParameter(":ID", OracleDbType.Int32)
                {
                    Direction = System.Data.ParameterDirection.Output
                };

            commandInsertar.Parameters.Add(parametroId);

            commandInsertar.ExecuteNonQuery();

            return Convert.ToInt32(parametroId.Value.ToString());
        }

        public void GuardarPartida(Partida partida)
        {
            using OracleConnection connection = conexion.ObtenerConexion();

            connection.Open();

            string sql = @"
                INSERT INTO PARTIDAS
                (
                    ID_JUGADOR,
                    PUNTUACION,
                    LONGITUD_SERPIENTE,
                    RESULTADO,
                    FECHA_PARTIDA
                )
                VALUES
                (
                    :ID_JUGADOR,
                    :PUNTUACION,
                    :LONGITUD,
                    :RESULTADO,
                    SYSDATE
                )";

            using OracleCommand command =
                new OracleCommand(sql, connection);

            command.Parameters.Add(
                ":ID_JUGADOR",
                OracleDbType.Int32
            ).Value = partida.IdJugador;

            command.Parameters.Add(
                ":PUNTUACION",
                OracleDbType.Int32
            ).Value = partida.Puntuacion;

            command.Parameters.Add(
                ":LONGITUD",
                OracleDbType.Int32
            ).Value = partida.LongitudSerpiente;

            command.Parameters.Add(
                ":RESULTADO",
                OracleDbType.Varchar2
            ).Value = partida.Resultado;

            command.ExecuteNonQuery();
        }

        public List<PartidaRanking> ObtenerRanking()
        {
            List<PartidaRanking> lista = new();

            using OracleConnection connection = conexion.ObtenerConexion();

            connection.Open();

            string sql = @"
                SELECT *
                FROM
                (
                    SELECT
                        J.NOMBRE,
                        P.PUNTUACION,
                        P.LONGITUD_SERPIENTE,
                        P.RESULTADO,
                        P.FECHA_PARTIDA
                    FROM PARTIDAS P
                    INNER JOIN JUGADORES J
                        ON J.ID_JUGADOR = P.ID_JUGADOR
                    ORDER BY P.PUNTUACION DESC
                )
                WHERE ROWNUM <= 10";

            using OracleCommand command =
                new OracleCommand(sql, connection);

            using OracleDataReader reader =
                command.ExecuteReader();

            while (reader.Read())
            {
                lista.Add(new PartidaRanking
                {
                    Nombre = reader["NOMBRE"].ToString() ?? "",
                    Puntuacion = Convert.ToInt32(reader["PUNTUACION"]),
                    Longitud = Convert.ToInt32(
                        reader["LONGITUD_SERPIENTE"]
                    ),
                    Resultado = reader["RESULTADO"].ToString() ?? "",
                    Fecha = Convert.ToDateTime(
                        reader["FECHA_PARTIDA"]
                    )
                });
            }

            return lista;
        }
    }

    public class PartidaRanking
    {
        public string Nombre { get; set; } = "";

        public int Puntuacion { get; set; }

        public int Longitud { get; set; }

        public string Resultado { get; set; } = "";

        public DateTime Fecha { get; set; }
    }
}
```

---

# 🖥️ 14. Forms/Form1.cs

```csharp
using SnakeOracle19C.Data;
using SnakeOracle19C.Game;
using SnakeOracle19C.Models;
using System.Drawing;

namespace SnakeOracle19C.Forms
{
    public partial class Form1 : Form
    {
        private const int TamanoCelda = 20;

        private const int Columnas = 30;

        private const int Filas = 25;

        private Serpiente serpiente;

        private Comida comida;

        private Timer timer;

        private int puntuacion;

        private bool jugando;

        private string nombreJugador = "";

        private readonly PartidaDAO partidaDAO;

        public Form1()
        {
            InitializeComponent();

            partidaDAO = new PartidaDAO();

            serpiente = new Serpiente(
                Columnas / 2,
                Filas / 2
            );

            comida = new Comida();

            timer = new Timer();

            timer.Interval = 120;

            timer.Tick += Timer_Tick;

            KeyPreview = true;

            KeyDown += Form1_KeyDown;

            InicializarJuego();
        }

        private void InicializarJuego()
        {
            serpiente = new Serpiente(
                Columnas / 2,
                Filas / 2
            );

            comida.Generar(
                Columnas,
                Filas,
                serpiente.Cuerpo
            );

            puntuacion = 0;

            jugando = false;

            lblPuntuacion.Text =
                "Puntuación: 0";

            lblLongitud.Text =
                "Longitud: " +
                serpiente.Cuerpo.Count;

            lblEstado.Text =
                "Presiona INICIAR";

            panelJuego.Invalidate();
        }

        private void btnIniciar_Click(
            object sender,
            EventArgs e)
        {
            if (string.IsNullOrWhiteSpace(txtJugador.Text))
            {
                MessageBox.Show(
                    "Ingresa el nombre del jugador.",
                    "Snake",
                    MessageBoxButtons.OK,
                    MessageBoxIcon.Warning
                );

                txtJugador.Focus();

                return;
            }

            nombreJugador = txtJugador.Text.Trim();

            InicializarJuego();

            jugando = true;

            timer.Start();

            lblEstado.Text = "Jugando...";

            txtJugador.Enabled = false;

            btnIniciar.Enabled = false;
        }

        private void btnReiniciar_Click(
            object sender,
            EventArgs e)
        {
            timer.Stop();

            txtJugador.Enabled = true;

            btnIniciar.Enabled = true;

            InicializarJuego();
        }

        private void Timer_Tick(
            object? sender,
            EventArgs e)
        {
            if (!jugando)
            {
                return;
            }

            bool comer =
                serpiente.ObtenerCabeza()
                == comida.Posicion;

            serpiente.Mover(comer);

            if (comer)
            {
                puntuacion += 10;

                comida.Generar(
                    Columnas,
                    Filas,
                    serpiente.Cuerpo
                );
            }

            if (ColisionConParedes() ||
                serpiente.ChocaConSuCuerpo())
            {
                FinalizarJuego();

                return;
            }

            lblPuntuacion.Text =
                "Puntuación: " +
                puntuacion;

            lblLongitud.Text =
                "Longitud: " +
                serpiente.Cuerpo.Count;

            panelJuego.Invalidate();
        }

        private bool ColisionConParedes()
        {
            Point cabeza =
                serpiente.ObtenerCabeza();

            return cabeza.X < 0 ||
                   cabeza.X >= Columnas ||
                   cabeza.Y < 0 ||
                   cabeza.Y >= Filas;
        }

        private void FinalizarJuego()
        {
            timer.Stop();

            jugando = false;

            lblEstado.Text =
                "GAME OVER";

            try
            {
                int idJugador =
                    partidaDAO.ObtenerOCrearJugador(
                        nombreJugador
                    );

                Partida partida = new Partida
                {
                    IdJugador = idJugador,

                    Puntuacion = puntuacion,

                    LongitudSerpiente =
                        serpiente.Cuerpo.Count,

                    Resultado = "GAME OVER",

                    FechaPartida = DateTime.Now
                };

                partidaDAO.GuardarPartida(partida);

                MessageBox.Show(
                    $"Game Over\n\n" +
                    $"Jugador: {nombreJugador}\n" +
                    $"Puntuación: {puntuacion}\n" +
                    $"Longitud: {serpiente.Cuerpo.Count}\n\n" +
                    "La partida fue guardada en Oracle.",
                    "Snake",
                    MessageBoxButtons.OK,
                    MessageBoxIcon.Information
                );
            }
            catch (Exception ex)
            {
                MessageBox.Show(
                    "La partida terminó, pero no fue posible " +
                    "guardar la información en Oracle.\n\n" +
                    ex.Message,
                    "Error Oracle",
                    MessageBoxButtons.OK,
                    MessageBoxIcon.Error
                );
            }

            txtJugador.Enabled = true;

            btnIniciar.Enabled = true;
        }

        private void Form1_KeyDown(
            object? sender,
            KeyEventArgs e)
        {
            if (!jugando)
            {
                return;
            }

            switch (e.KeyCode)
            {
                case Keys.Up:

                    if (serpiente.DireccionActual
                        != Direccion.Abajo)
                    {
                        serpiente.DireccionActual =
                            Direccion.Arriba;
                    }

                    break;

                case Keys.Down:

                    if (serpiente.DireccionActual
                        != Direccion.Arriba)
                    {
                        serpiente.DireccionActual =
                            Direccion.Abajo;
                    }

                    break;

                case Keys.Left:

                    if (serpiente.DireccionActual
                        != Direccion.Derecha)
                    {
                        serpiente.DireccionActual =
                            Direccion.Izquierda;
                    }

                    break;

                case Keys.Right:

                    if (serpiente.DireccionActual
                        != Direccion.Izquierda)
                    {
                        serpiente.DireccionActual =
                            Direccion.Derecha;
                    }

                    break;
            }
        }

        private void panelJuego_Paint(
            object? sender,
            PaintEventArgs e)
        {
            Graphics g = e.Graphics;

            g.Clear(Color.Black);

            using Brush brushSerpiente =
                new SolidBrush(Color.LimeGreen);

            using Brush brushCabeza =
                new SolidBrush(Color.Green);

            using Brush brushComida =
                new SolidBrush(Color.Red);

            foreach (Point punto in serpiente.Cuerpo)
            {
                Rectangle rect = new Rectangle(
                    punto.X * TamanoCelda,
                    punto.Y * TamanoCelda,
                    TamanoCelda - 1,
                    TamanoCelda - 1
                );

                g.FillRectangle(
                    punto == serpiente.ObtenerCabeza()
                        ? brushCabeza
                        : brushSerpiente,
                    rect
                );
            }

            Rectangle comidaRect =
                new Rectangle(
                    comida.Posicion.X * TamanoCelda,
                    comida.Posicion.Y * TamanoCelda,
                    TamanoCelda - 1,
                    TamanoCelda - 1
                );

            g.FillEllipse(
                brushComida,
                comidaRect
            );
        }

        private void btnRanking_Click(
            object sender,
            EventArgs e)
        {
            FrmRanking ranking =
                new FrmRanking();

            ranking.ShowDialog();
        }
    }
}
```

---

# 🎨 15. Forms/Form1.Designer.cs

```csharp
namespace SnakeOracle19C.Forms
{
    partial class Form1
    {
        private System.ComponentModel.IContainer components = null;

        private Panel panelJuego;

        private TextBox txtJugador;

        private Label lblJugador;

        private Label lblPuntuacion;

        private Label lblLongitud;

        private Label lblEstado;

        private Button btnIniciar;

        private Button btnReiniciar;

        private Button btnRanking;

        protected override void Dispose(bool disposing)
        {
            if (disposing &&
                (components != null))
            {
                components.Dispose();
            }

            base.Dispose(disposing);
        }

        private void InitializeComponent()
        {
            panelJuego = new Panel();
            txtJugador = new TextBox();
            lblJugador = new Label();
            lblPuntuacion = new Label();
            lblLongitud = new Label();
            lblEstado = new Label();
            btnIniciar = new Button();
            btnReiniciar = new Button();
            btnRanking = new Button();

            SuspendLayout();

            // lblJugador
            lblJugador.AutoSize = true;
            lblJugador.Location = new Point(20, 20);
            lblJugador.Text = "Jugador:";

            // txtJugador
            txtJugador.Location = new Point(85, 17);
            txtJugador.Size = new Size(180, 27);

            // lblPuntuacion
            lblPuntuacion.AutoSize = true;
            lblPuntuacion.Location = new Point(300, 20);
            lblPuntuacion.Text = "Puntuación: 0";

            // lblLongitud
            lblLongitud.AutoSize = true;
            lblLongitud.Location = new Point(450, 20);
            lblLongitud.Text = "Longitud: 3";

            // lblEstado
            lblEstado.AutoSize = true;
            lblEstado.Font =
                new Font(
                    "Segoe UI",
                    10F,
                    FontStyle.Bold
                );

            lblEstado.Location =
                new Point(650, 20);

            lblEstado.Text =
                "Presiona INICIAR";

            // panelJuego
            panelJuego.BorderStyle =
                BorderStyle.FixedSingle;

            panelJuego.Location =
                new Point(20, 60);

            panelJuego.Size =
                new Size(600, 500);

            panelJuego.Paint +=
                panelJuego_Paint;

            // btnIniciar
            btnIniciar.Location =
                new Point(650, 80);

            btnIniciar.Size =
                new Size(150, 40);

            btnIniciar.Text =
                "INICIAR";

            btnIniciar.Click +=
                btnIniciar_Click;

            // btnReiniciar
            btnReiniciar.Location =
                new Point(650, 140);

            btnReiniciar.Size =
                new Size(150, 40);

            btnReiniciar.Text =
                "REINICIAR";

            btnReiniciar.Click +=
                btnReiniciar_Click;

            // btnRanking
            btnRanking.Location =
                new Point(650, 200);

            btnRanking.Size =
                new Size(150, 40);

            btnRanking.Text =
                "RANKING";

            btnRanking.Click +=
                btnRanking_Click;

            // Form1
            AutoScaleDimensions =
                new SizeF(7F, 15F);

            AutoScaleMode =
                AutoScaleMode.Font;

            ClientSize =
                new Size(850, 600);

            Controls.Add(lblJugador);
            Controls.Add(txtJugador);
            Controls.Add(lblPuntuacion);
            Controls.Add(lblLongitud);
            Controls.Add(lblEstado);
            Controls.Add(panelJuego);
            Controls.Add(btnIniciar);
            Controls.Add(btnReiniciar);
            Controls.Add(btnRanking);

            FormBorderStyle =
                FormBorderStyle.FixedSingle;

            MaximizeBox = false;

            StartPosition =
                FormStartPosition.CenterScreen;

            Text =
                "🐍 Snake - Oracle 19c";

            ResumeLayout(false);
            PerformLayout();
        }
    }
}
```

---

# 🏆 16. Forms/FrmRanking.cs

```csharp
using SnakeOracle19C.Data;

namespace SnakeOracle19C.Forms
{
    public partial class FrmRanking : Form
    {
        private readonly PartidaDAO partidaDAO;

        public FrmRanking()
        {
            InitializeComponent();

            partidaDAO = new PartidaDAO();

            CargarRanking();
        }

        private void CargarRanking()
        {
            try
            {
                List<PartidaRanking> ranking =
                    partidaDAO.ObtenerRanking();

                dgvRanking.Rows.Clear();

                foreach (PartidaRanking partida in ranking)
                {
                    dgvRanking.Rows.Add(
                        partida.Nombre,
                        partida.Puntuacion,
                        partida.Longitud,
                        partida.Resultado,
                        partida.Fecha.ToString(
                            "dd/MM/yyyy HH:mm"
                        )
                    );
                }
            }
            catch (Exception ex)
            {
                MessageBox.Show(
                    "No fue posible consultar el ranking.\n\n" +
                    ex.Message,
                    "Oracle 19c",
                    MessageBoxButtons.OK,
                    MessageBoxIcon.Error
                );
            }
        }

        private void btnActualizar_Click(
            object sender,
            EventArgs e)
        {
            CargarRanking();
        }
    }
}
```

---

# 🏆 17. Forms/FrmRanking.Designer.cs

```csharp
namespace SnakeOracle19C.Forms
{
    partial class FrmRanking
    {
        private System.ComponentModel.IContainer components = null;

        private DataGridView dgvRanking;

        private Button btnActualizar;

        protected override void Dispose(bool disposing)
        {
            if (disposing &&
                (components != null))
            {
                components.Dispose();
            }

            base.Dispose(disposing);
        }

        private void InitializeComponent()
        {
            dgvRanking = new DataGridView();
            btnActualizar = new Button();

            ((System.ComponentModel.ISupportInitialize)
                dgvRanking).BeginInit();

            SuspendLayout();

            // dgvRanking
            dgvRanking.AllowUserToAddRows = false;

            dgvRanking.AllowUserToDeleteRows = false;

            dgvRanking.AutoSizeColumnsMode =
                DataGridViewAutoSizeColumnsMode.Fill;

            dgvRanking.ColumnHeadersHeightSizeMode =
                DataGridViewColumnHeadersHeightSizeMode.AutoSize;

            dgvRanking.Location =
                new Point(20, 20);

            dgvRanking.ReadOnly = true;

            dgvRanking.RowHeadersVisible = false;

            dgvRanking.Size =
                new Size(760, 350);

            dgvRanking.Columns.Add(
                "Jugador",
                "Jugador"
            );

            dgvRanking.Columns.Add(
                "Puntuacion",
                "Puntuación"
            );

            dgvRanking.Columns.Add(
                "Longitud",
                "Longitud"
            );

            dgvRanking.Columns.Add(
                "Resultado",
                "Resultado"
            );

            dgvRanking.Columns.Add(
                "Fecha",
                "Fecha"
            );

            // btnActualizar
            btnActualizar.Location =
                new Point(20, 390);

            btnActualizar.Size =
                new Size(150, 40);

            btnActualizar.Text =
                "ACTUALIZAR";

            btnActualizar.Click +=
                btnActualizar_Click;

            // FrmRanking
            AutoScaleDimensions =
                new SizeF(7F, 15F);

            AutoScaleMode =
                AutoScaleMode.Font;

            ClientSize =
                new Size(800, 450);

            Controls.Add(dgvRanking);

            Controls.Add(btnActualizar);

            StartPosition =
                FormStartPosition.CenterParent;

            Text =
                "Ranking - Snake Oracle 19c";

            ((System.ComponentModel.ISupportInitialize)
                dgvRanking).EndInit();

            ResumeLayout(false);
        }
    }
}
```

---

# 🚀 18. Program.cs

```csharp
namespace SnakeOracle19C
{
    internal static class Program
    {
        [STAThread]
        static void Main()
        {
            ApplicationConfiguration.Initialize();

            Application.Run(
                new Forms.Form1()
            );
        }
    }
}
```

---

# ⚠️ 19. Ajuste importante de los archivos Game

En `Serpiente.cs` debe estar incluido el namespace:

```csharp
using System.Drawing;

namespace SnakeOracle19C.Game
{
    public class Serpiente
    {
        // ...
    }
}
```

Y `Direccion.cs`:

```csharp
namespace SnakeOracle19C.Game
{
    public enum Direccion
    {
        Arriba,
        Abajo,
        Izquierda,
        Derecha
    }
}
```

---

# 🧪 20. Probar Oracle

Antes de ejecutar el juego, verificar que Oracle esté disponible.

Ejemplo:

```text
localhost:1521/orcl
```

La cadena de conexión:

```text
Data Source=localhost:1521/orcl
```

debe coincidir con la configuración de Oracle 19c.

---

# ▶️ 21. Ejecutar el proyecto

En Visual Studio:

```text
Build
→ Build Solution
```

Después:

```text
Debug
→ Start Without Debugging
```

También se puede utilizar:

```text
Ctrl + F5
```

---

# 🎮 22. Cómo jugar

1. Escribir el nombre del jugador.
2. Presionar `INICIAR`.
3. Utilizar:

```text
       ↑
       |
← -----+----- →
       |
       ↓
```

4. Comer los cuadrados rojos.
5. Cada comida suma:

```text
+10 puntos
```

6. La serpiente aumenta de tamaño.
7. Si toca una pared termina la partida.
8. Si toca su propio cuerpo termina la partida.
9. Al terminar, la partida se almacena en Oracle 19c.

---

# 🏆 23. Consultar resultados directamente en Oracle

Jugadores:

```sql
SELECT
    ID_JUGADOR,
    NOMBRE,
    FECHA_REGISTRO
FROM JUGADORES
ORDER BY ID_JUGADOR;
```

Partidas:

```sql
SELECT
    ID_PARTIDA,
    ID_JUGADOR,
    PUNTUACION,
    LONGITUD_SERPIENTE,
    RESULTADO,
    FECHA_PARTIDA
FROM PARTIDAS
ORDER BY ID_PARTIDA DESC;
```

Ranking:

```sql
SELECT
    J.NOMBRE AS JUGADOR,
    P.PUNTUACION,
    P.LONGITUD_SERPIENTE,
    P.RESULTADO,
    P.FECHA_PARTIDA
FROM PARTIDAS P
INNER JOIN JUGADORES J
    ON P.ID_JUGADOR = J.ID_JUGADOR
ORDER BY P.PUNTUACION DESC;
```

---

# 🧹 24. Limpiar información de pruebas

Para eliminar partidas:

```sql
DELETE FROM PARTIDAS;

COMMIT;
```

Para eliminar jugadores:

```sql
DELETE FROM JUGADORES;

COMMIT;
```

Si existen partidas relacionadas, primero eliminar:

```sql
DELETE FROM PARTIDAS;

DELETE FROM JUGADORES;

COMMIT;
```

---

# 🧠 25. Funcionamiento de la aplicación

La arquitectura funciona de la siguiente manera:

```text
                    ┌─────────────────┐
                    │   Windows Forms │
                    │      Form1      │
                    └────────┬────────┘
                             │
                             ▼
                    ┌─────────────────┐
                    │   Game Engine   │
                    │                 │
                    │ Serpiente       │
                    │ Comida          │
                    │ Dirección       │
                    └────────┬────────┘
                             │
                             ▼
                    ┌─────────────────┐
                    │   PartidaDAO    │
                    └────────┬────────┘
                             │
                             ▼
                    ┌─────────────────┐
                    │ ConexionOracle  │
                    └────────┬────────┘
                             │
                             ▼
                    ┌─────────────────┐
                    │    Oracle 19c   │
                    │                 │
                    │   JUGADORES     │
                    │   PARTIDAS      │
                    └─────────────────┘
```

---

# 📊 26. Flujo de una partida

```text
INICIO
   │
   ▼
Ingresar jugador
   │
   ▼
Presionar INICIAR
   │
   ▼
Crear serpiente
   │
   ▼
Generar comida
   │
   ▼
Mover serpiente
   │
   ├──── ¿Comió?
   │       │
   │       ├── SI → +10 puntos
   │       │          │
   │       │          ▼
   │       │       Crecer
   │       │
   │       └── NO
   │
   ▼
¿Colisión?
   │
   ├── NO → continuar
   │
   └── SI
        │
        ▼
    GAME OVER
        │
        ▼
 Guardar partida
        │
        ▼
   Oracle 19c
```

---

# 🔐 27. Recomendación para la conexión

Para un proyecto académico se puede utilizar:

```csharp
private readonly string connectionString =
    "User Id=SYSTEM;" +
    "Password=TU_PASSWORD;" +
    "Data Source=localhost:1521/orcl;";
```

Para un proyecto real, no es recomendable dejar la contraseña directamente dentro del código fuente.

Se recomienda posteriormente utilizar:

```text
appsettings.json
```

o variables de entorno.

---

# 🧪 28. Datos de prueba

Se pueden insertar jugadores manualmente:

```sql
INSERT INTO JUGADORES
(
    NOMBRE
)
VALUES
(
    'Giovanny'
);

INSERT INTO JUGADORES
(
    NOMBRE
)
VALUES
(
    'Carlos'
);

INSERT INTO JUGADORES
(
    NOMBRE
)
VALUES
(
    'Maria'
);

COMMIT;
```

Partidas:

```sql
INSERT INTO PARTIDAS
(
    ID_JUGADOR,
    PUNTUACION,
    LONGITUD_SERPIENTE,
    RESULTADO
)
VALUES
(
    1,
    150,
    18,
    'GAME OVER'
);

INSERT INTO PARTIDAS
(
    ID_JUGADOR,
    PUNTUACION,
    LONGITUD_SERPIENTE,
    RESULTADO
)
VALUES
(
    2,
    90,
    12,
    'GAME OVER'
);

INSERT INTO PARTIDAS
(
    ID_JUGADOR,
    PUNTUACION,
    LONGITUD_SERPIENTE,
    RESULTADO
)
VALUES
(
    3,
    210,
    24,
    'GAME OVER'
);

COMMIT;
```

---

# 🔍 29. Verificar Top 10

```sql
SELECT *
FROM
(
    SELECT
        J.NOMBRE AS JUGADOR,
        P.PUNTUACION,
        P.LONGITUD_SERPIENTE,
        P.RESULTADO,
        P.FECHA_PARTIDA
    FROM PARTIDAS P
    INNER JOIN JUGADORES J
        ON P.ID_JUGADOR = J.ID_JUGADOR
    ORDER BY P.PUNTUACION DESC
)
WHERE ROWNUM <= 10;
```

---

# 🐍 30. Resultado esperado

La aplicación tendrá una interfaz similar a:

```text
┌───────────────────────────────────────────────────────────────┐
│                    🐍 SNAKE ORACLE 19C                        │
├───────────────────────────────────────────────────────────────┤
│ Jugador: Giovanny       Puntuación: 120    Longitud: 15       │
│                                                               │
│ ┌───────────────────────────────────────┐                     │
│ │                                       │                     │
│ │           🟩🟩🟩                     │                     │
│ │                 🟩                    │                     │
│ │                 🟩  🔴                │                     │
│ │                                       │                     │
│ │                                       │                     │
│ └───────────────────────────────────────┘                     │
│                                                               │
│        [ INICIAR ]                                            │
│        [ REINICIAR ]                                          │
│        [ RANKING ]                                             │
└───────────────────────────────────────────────────────────────┘
```

---

# 🏆 31. Ranking

La ventana de ranking:

```text
┌─────────────────────────────────────────────────────────────┐
│                 RANKING SNAKE                                │
├───────────┬────────────┬──────────┬────────────┬─────────────┤
│ Jugador   │ Puntuación │ Longitud │ Resultado  │ Fecha       │
├───────────┼────────────┼──────────┼────────────┼─────────────┤
│ Giovanny  │    210     │    24    │ GAME OVER  │ 17/09/2026  │
│ Carlos    │    150     │    18    │ GAME OVER  │ 17/09/2026  │
│ Maria     │     90     │    12    │ GAME OVER  │ 17/09/2026  │
└───────────┴────────────┴──────────┴────────────┴─────────────┘

[ ACTUALIZAR ]
```

---

# 🛠️ 32. Solución de problemas

## Error de conexión Oracle

Si aparece:

```text
ORA-12154
```

verificar:

```text
localhost:1521/orcl
```

y el nombre del servicio de Oracle.

---

## Error ORA-01017

```text
ORA-01017: invalid username/password
```

Verificar:

```text
User Id
Password
```

en:

```text
ConexionOracle.cs
```

---

## Error ORA-12541

```text
ORA-12541: TNS:no listener
```

Verificar que Oracle Listener esté ejecutándose.

---

## No aparece el juego

Verificar que el proyecto sea:

```text
Windows Forms App
```

y que el `.csproj` contenga:

```xml
<UseWindowsForms>true</UseWindowsForms>
```

---

# 📌 33. Mejoras futuras

Este proyecto puede ampliarse posteriormente con:

* 🔊 Sonidos.
* 🎵 Música.
* 🏆 Top 10.
* ⭐ Diferentes niveles.
* ⚡ Incremento de velocidad.
* 🧱 Obstáculos.
* 🍎 Diferentes tipos de comida.
* ❤️ Sistema de vidas.
* 👤 Login de jugadores.
* 📊 Estadísticas.
* 📅 Historial de partidas.
* 🥇 Medallas.
* 🌙 Modo oscuro.
* 🎨 Diferentes skins para la serpiente.
* 🗄️ Stored Procedures de Oracle.
* 🔐 Configuración segura de credenciales.

---

# 📚 34. Objetivo académico

Este proyecto permite practicar simultáneamente:

```text
C#
 │
 ├── Clases
 ├── Objetos
 ├── Eventos
 ├── Windows Forms
 ├── Timer
 ├── List<T>
 ├── Programación orientada a objetos
 │
 └──────────────┐
                │
                ▼
          Oracle Database
                │
                ├── SQL
                ├── INSERT
                ├── SELECT
                ├── JOIN
                ├── Primary Key
                ├── Foreign Key
                └── Identity
```

---

# 👨‍💻 Autor

Proyecto académico desarrollado con:

```text
C#
Visual Studio 2026
.NET 10
Windows Forms
Oracle Database 19c
```

## 🐍 Snake Oracle 19c

```text
Jugar → Procesar → Guardar → Consultar → Ranking
```
:. . / .
